views:
  - type: table
    name: Table
    filters:
      and:
        - '!file.folder.startsWith("Images")'
        - status != "dirty"
    order:
      - file.name
      - status
    sort:
      - property: status
        direction: ASC
